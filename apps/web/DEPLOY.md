# Deploying apps/web

This app deploys to Vercel (project `sealed-api`, root directory `apps/web`,
Next.js preset). Full instructions and required environment variables:
see [docs/DEPLOYMENT.md](../../docs/DEPLOYMENT.md) at the repo root.
